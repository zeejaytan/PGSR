# What we are trying to establish with PGSR

Prefix **`R`** (reserved 2026-09-09; U/C/G/M/O/P/S taken, R free and mnemonic).
Numbers never reused.

No `R` question is opened yet. The first work here answers MILo
[`M8`](../../MILo/intent/M8-does-pgsr-replace-milo.md) — does PGSR replace MILo as
the sherd mesh route at the resolution a break face needs? — and its tickets carry
`Answers: M8` until an `R` question supersedes it by amendment, never by duplication.

| # | Question | Status | Blocked by |
|---|---|---|---|
| [R1](R1-does-qgs-preserve-edges-beyond-pgsr.md) | Does QGS preserve break-face edges beyond PGSR under PGSR-identical conditions? | open — one capture, one seed, PGSR variant A as control | M8 (answered NO 2026-09-13, follow-on) |

This folder is **state, not a log**. Edit a line when it turns out wrong; git holds
the history. Runs live in `docs/notes/`.
