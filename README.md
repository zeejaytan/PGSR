# PGSR — sherd surfacing trial

Standalone PGSR judged as a sherd mesh route on Rabati turntable photographs.
Sibling of MILo, never inside it. Working rules: [`AGENTS.md`](AGENTS.md).
First verdict answers MILo `intent/M8`; new questions take prefix `R` (see
[`intent/`](intent/)).
